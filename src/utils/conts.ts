export enum AppRoutes {
  Root = '/',
  SignIn = '/signin',
  SignUp = '/signup',
}

export enum HTTP_CODE {
  BAD_REQUEST = 400,
  UNAUTHORIZED = 401,
  NOT_FOUND = 404,
}

export enum NameSpace {
  data = 'DATA',
  user = 'USER',
  app = 'APP',
}
export const steps = ['Контакты', 'Личные данные', 'Параметры', 'Предпочтения', 'Платежи', 'Готово!'];
