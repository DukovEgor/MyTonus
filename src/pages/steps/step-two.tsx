import { Button, Container, Typography } from '@mui/material';

export default function StepTwo() {
  return (
    <Container>
      <Typography>
        Вторая страница
      </Typography>
      <Button variant="contained" color="error">Назад</Button>
    </Container>
  );
}
